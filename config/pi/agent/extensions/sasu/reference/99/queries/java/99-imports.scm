(import_declaration
  (scoped_identifier) @import.name) @import.decl

(import_declaration
  "static"
  (scoped_identifier) @import.name) @import.decl

;; idk about wildcards, but this covers most for now
